import 'package:flutter/material.dart';

class BlankPage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Blank Page 1'),
        backgroundColor: Colors.yellow[700],
      ),
      body: Center(
        child: Text('Future feature will be implemented here.'),
      ),
    );
  }
}

// Repeat similar code for BlankPage2
