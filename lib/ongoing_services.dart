import 'package:flutter/material.dart';

class OngoingServicesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ongoing Services'),
      ),
      body: Center(
        child: Text('This is the Ongoing Services Page'),
      ),
    );
  }
}
